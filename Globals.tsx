import { ApolloClient, InMemoryCache } from "@apollo/client";

export const colors = {
  primary: '#d25',
  black: '#000',
  white: '#fff',
  dark: '#222',
  lightGrey: '#ccc',
  primaryLight: '#f47',
  primaryDark: '#b03'
};

export const icons = {
  close: 'close',
  swap: 'swap',
  cli: 'codesquareo',
  pic: 'picture'
};
